import Vue from "vue";
import { Button, Tabbar, TabbarItem, NavBar, Icon, Swipe, SwipeItem, Grid, GridItem, Cell, List, Card, Image, Tag, Rate } from "vant";
Vue.use(Button).use(Tabbar).use(TabbarItem).use(NavBar).use(Icon).use(Swipe).use(SwipeItem).use(Grid).use(GridItem)
    .use(Cell).use(List).use(Card).use(Image).use(Tag).use(Rate);